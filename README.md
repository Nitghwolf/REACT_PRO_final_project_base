«Структура папок»: по FSD кроме папки entities

# Сделано и частично сделано:
1. Архитектура и структура:
- [x] shared/ui только презентационные компоненты, без стора и API
- [x] Изоляция фич по папкам, слои shared/, features/, pages/ соблюдены
- [ ] Устранены лишние prop-chains (Не искал, возможно устранил)

2. Оптимизация рендеров:
- [x] Уместный React.memo (Субъективная оценка, не могу сказать на сколько и где было уместно)
- [x] Точечные useMemo/useCallback для тяжелых вычислений/колбэков в списках (1.5) (тяжелых мест не было)

3. React.Portal (модалка, доступность):
- [x] Портал работает, #modal-root добавлен, ESC/overlay обрабатываются
- [x] Корректный фокус: установка при открытии, возврат на триггер

4. useRef:
- [x] Автофокус/DOM-интеракция (На модалке, на инпутах не работает)

5. Альтернативная сборка (esbuild/SWC/Vite):
- [x] Рабочая сборка

7. README:
- [x] «Что сделано» + «Структура папок»

# Не сделано:
2. Оптимизация рендеров:
- [] Profiler: найден hotspot и описан кратко (Не искал и не описывал)
4. useRef: 
- [] Хранение состояния между рендерами без лишних рендеров (не еашел применение)
5. Альтернативная сборка (esbuild/SWC/Vite):
- [] Сравнение с Webpack в README (время/размер, краткий вывод) (не сравнивал, работает быстрее)
6. React 19 hooks (useActionState/useOptimistic) — 3 балла
- [] Реальный UX-кейс с одним из хуков (2)
- [] Обработка ошибок/ресет/оптимистичность (1)
